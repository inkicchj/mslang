可以把 **mslang 0.3** 明确定位为“**学术语义能力版本**”，而不是继续做底层架构重构。0.2.x 已经把 `Source → Parse → Normalize → Runtime → Semantic → Render` 这条主线基本稳定下来，因此 0.3 最重要的目标应该是：**让 mslang 从“支持论文写作的标记语言”进一步变成“能够理解论文结构、引用关系和来源位置的学术文档 DSL”。** 当前已有标题、图表公式、定理、脚注、文献/术语引用、跨文档、Diagnostics 和 SemanticModel，这已经足够支撑这一演进。

## 1. 0.3 的核心主题：论文结构语义化

目前论文主要还是由 Heading 和普通 Paragraph 拼出来。0.3 不需要增加大量新的块语法，但应该让 Document 能保存真正的论文元数据，例如：

```js
document.meta = {
  title: "...",
  authors: [...],
  affiliations: [...],
  abstract: "...",
  keywords: [...],
  language: "zh-CN"
}
```

语法可以保持非常轻，例如通过文档头部：

```text
@meta({
  title: "生成式AI介入下知识螺旋何以持续？",
  authors: ["张三"],
  keywords: ["生成式AI", "知识螺旋"]
})
```

摘要、关键词、附录等如果需要块级结构，可以只增加极少数真正具有学术语义的节点，例如 `AbstractBlock`、`AppendixBlock`，而不是给每种视觉格式都创造 AST 类型。这样 AI、导出器和编辑器就能明确知道“这是摘要”，而不是通过标题文字猜测。

## 2. 文献引用系统：0.3 应直接转向 Citation.js + CSL

你提到的 **Citation.js / `@citation-js/plugin-csl` 非常适合 mslang**，而且我认为这会比自己继续维护 APA、GB/T、IEEE 等规则合理得多。

Citation.js 本身就是模块化的文献数据转换工具，可以把 BibTeX、BibJSON 等格式统一转换到 CSL-JSON，并通过插件产生格式化引用；官方当前推荐核心包 `@citation-js/core` 加所需插件，而 `@citation-js/plugin-csl` 负责 CSL 格式化，底层使用 `citeproc-js`。 `@citation-js/plugin-csl` 当前 npm 最新版为 0.8.2，并原生支持 bibliography/citation 格式化以及 HTML、text、RTF 等输出。

所以 0.3 **不要再扩展现在自己的 `_formatBibEntry()` 一类规则**。现有：

```text
numeric
author-year
author
gbt7714
```

可以保留为兼容模式，但新的设计应该变成：

```text
mslang
  ↓
CitationAdapter
  ↓
Citation.js
  ↓
CSL-JSON
  ↓
plugin-csl / citeproc-js
  ↓
citation + bibliography
```

比较合适的 Runtime 数据模型是直接逐步向 CSL-JSON 靠拢：

```js
data: {
  bibliography: {
    smith2024: {
      id: "smith2024",
      type: "article-journal",
      title: "...",
      author: [
        { family: "Smith", given: "John" }
      ],
      issued: {
        "date-parts": [[2024]]
      },
      "container-title": "..."
    }
  }
}
```

Citation.js 本身就接受 CSL-JSON，同时也可通过相应插件解析 BibTeX 等输入。

这样未来：

```js
render(src, {
  citation: {
    style: "apa",
    locale: "en-US"
  }
})
```

甚至：

```js
citation: {
  style: customCSL
}
```

就有了清晰扩展点。

### 一个重要建议

**不要让 Citation.js 接管 mslang 的引用语义。**

仍然应该：

```text
SemanticModel
负责：
- 谁引用了谁
- citation 出现顺序
- missing citation
- block dependency
- source span

Citation.js
只负责：
- 文献数据标准化
- citation 文本格式
- bibliography 文本格式
```

也就是：

```text
Semantic = meaning
Citation.js = formatting
```

这个边界非常重要，否则以后 diagnostics 和 BlockEditor 会被第三方格式化库绑死。

## 3. 增加 Citation Adapter，而不是把 Citation.js 散布到 Renderer

建议新增：

```text
src/
  citation.js
```

或者：

```text
citation/
  adapter.js
```

但以 mslang 当前轻量结构，我更推荐一个文件：

```js
export class CitationEngine {
  formatCitation(keys, context) {}
  formatBibliography(keys, context) {}
  normalize(entries) {}
}
```

默认实现用 Citation.js。

Renderer 只做：

```js
this.citation.formatCitation(...)
```

SemanticAnalyzer 完全不 import Citation.js。

这样以后想替换 CSL engine，不影响 AST 和 Semantic。

---

## 4. Source Mapping 应成为 0.3 的第二个核心能力

现在节点级 span 已经能精确定位 `@cite/@ref/FootnoteRef`，但多文件 `@include()` 后还缺最关键的信息：

```text
这个 span 来自哪个文件？
```

0.3 建议把：

```js
span: {
  start: 120,
  end: 138
}
```

升级成：

```js
span: {
  sourceId: "chapter2.msl",
  start: 120,
  end: 138
}
```

未来再需要时增加：

```js
line
column
```

即可。

IncludeExpander 不应该简单把所有文本拼成一个失去来源的信息字符串，而应该同时维护：

```js
SourceMap
```

于是 diagnostics 能输出：

```js
{
  code: "missing-citation",
  message: "...",
  span: {
    sourceId: "chapter2.msl",
    start: 832,
    end: 850
  }
}
```

这样编辑器和 AI 才能真正做到：

> “打开 chapter2.msl，并修改第 832–850 字符。”

这会比继续新增语法有价值很多。

---

## 5. 修正脚注语义

这个可以正式在 0.3 改掉。

当前同一个：

```text
[^note]
```

引用两次，会得到两个编号。项目测试已经明确记录了这一现状，并留待 0.3 决定。

我建议 0.3 定义为：

> **Footnote label 唯一决定 footnote number。**

例如：

```text
这里引用[^a]。

后面再次引用[^a]。
```

两处都显示：

```text
1
```

并且脚注正文只出现一次。

如果希望体验更完整，可以生成：

```text
↩︎¹ ↩︎²
```

多个 backlink，但这个属于 Renderer 层增强。

---

# 6. SemanticModel 从“编号表”升级为论文分析模型

这是我认为 0.3 最有价值、也最符合 AI 文献工作台定位的地方。

现在 SemanticModel 已经拥有：

```text
refs
citeNumbers
citeOrder
termOrder
headingSeq
diagnostics
```

下一步可以增加：

```js
semantic.outline
semantic.sections
semantic.references
semantic.figures
semantic.tables
semantic.equations
semantic.theorems
semantic.footnotes
```

以及统计信息：

```js
semantic.stats = {
  words,
  citations,
  figures,
  tables,
  equations,
  sections
}
```

这里不要增加新语法。

直接从已有 AST 推导即可。

例如：

```js
analyze(source)
```

未来可以返回：

```js
{
  document,
  semantic: {
    outline: [...],
    citations: {...},
    references: {...},
    stats: {...}
  },
  diagnostics
}
```

于是 AI 工作台可以直接回答：

```text
第二章引用了哪些论文？
哪一节没有引用任何文献？
哪些图没有在正文中被引用？
有哪些参考文献从未使用？
术语第一次在哪里出现？
```

这才是 mslang 相对于 Markdown 真正有价值的差异。

---

# 7. Diagnostics 从“错误检查”扩展到“学术一致性检查”

现在的：

```text
missing-citation
missing-reference
duplicate-label
orphan-caption
...
```

已经很好。

0.3 可以增加一些非常轻量、确定性的 semantic diagnostics，比如：

```text
unused-bibliography
unused-label
unreferenced-figure
unreferenced-table
duplicate-citation-id
missing-title
missing-abstract
missing-keywords
empty-section
```

但不要做：

```text
“理论贡献不足”
“文献综述质量较差”
```

这种需要 AI 判断的东西。

原则应该是：

> **语言核心只做确定性检查，主观学术评价交给 AI 工作台。**

---

# 8. AI 能力不要进入语言语法

这一点我建议在 0.3 明确冻结。

不要出现：

```text
@ai(...)
@generate(...)
@summarize(...)
```

这种语言结构。

更合理的是：

```text
mslang
→ analyze()
→ SemanticModel
→ AI application
```

AI 得到：

```text
AST
SemanticModel
Diagnostics
SourceMap
```

再做：

```text
rewrite
repair
suggest
summarize
```

这样 mslang 本身仍然是确定性的。

现在已有 `llmReport()` 的方向就是合理的：语言提供结构化事实，AI 做解释和修复。

---

# 9. 输出后端：0.3 可以建立接口，但不必马上支持所有格式

现在 HTMLRenderer 已经非常成熟。

0.3 可以正式抽象：

```text
Renderer
```

概念：

```js
render(document, context)
```

然后：

```text
HTMLRenderer
```

作为默认后端。

但我不建议 0.3 同时做：

```text
DOCX
PDF
LaTeX
EPUB
```

那会迅速失控。

如果需要验证 AST 是否真正独立于 HTML，我最推荐先做一个：

```text
LaTeXRenderer
```

作为实验性第二 Renderer。

因为论文语义：

```text
section
equation
theorem
cite
figure
table
bibliography
```

与 LaTeX 天然对应。

如果 LaTeXRenderer 很难实现，就说明 AST 仍然混入了过多 HTML 假设。

---

# 10. `@plugin`、宏和表达式系统在 0.3 应冻结

现在已有：

```text
@let
@if
@define/@use
host functions
@plugin
```

已经足够。

尤其 Citation.js 加进来以后，更没有必要让 mslang 自己朝程序语言方向走。

因此明确不增加：

```text
for
while
lambda
class
module
import
scope
async syntax
```

`@plugin` 继续保持：

```text
default false
```

作为高级 escape hatch 即可。

---

# 11. 0.3 推荐最终架构

不需要改变 0.2.2 的主架构，只是在 Semantic 周围增加能力：

```text
                 Source
                   ↓
             IncludeExpander
                   ↓
                Lexer
                   ↓
                Parser
                   ↓
             Normalizer
                   ↓
              Stable AST
                   ↓
             RuntimeContext
                   ↓
            SemanticAnalyzer
             ↙      ↓       ↘
       SourceMap  Citation   Diagnostics
                    ↓
              CitationEngine
              (Citation.js)
                    ↓
             PreparedDocument
              ↙            ↘
        analyze()          Renderer
                              ↓
                         HTML / LaTeX
```

这里核心仍然是：

```text
PreparedDocument
```

建议 0.3 正式包含：

```js
{
  document,
  runtime,
  semantic,
  sourceMap,
  issues
}
```

---

# 12. 0.3 推荐模块变化

保持现在扁平结构，只建议新增两三个模块：

```text
src/
├── lexer.js
├── parser.js
├── normalize.js
├── nodes.js
├── expression.js
├── runtime.js
│
├── semantic.js
├── citation.js        ← 新
├── source-map.js      ← 新
│
├── prepare.js
├── renderer.js
├── builtin.js
├── include.js
├── blockeditor.js
└── ...
```

不要创建：

```text
compiler/
runtime/
semantic/
renderer/
citation/
...
```

一堆目录。

目前项目规模还不需要。

---

# 13. Citation.js 的具体集成建议

依赖推荐模块化版本：

```bash
npm install @citation-js/core @citation-js/plugin-csl
```

如果以后需要 BibTeX：

```bash
npm install @citation-js/plugin-bibtex
```

Citation.js 官方就是按 core + plugin 模式设计，并支持浏览器与 Node.js；CSL 插件负责通过 citeproc-js 输出格式化 citation/bibliography。

不建议直接：

```bash
npm install citation-js
```

然后把所有插件打进 bundle。

因为 mslang 本来就强调轻量，Citation.js 的模块化包更符合这个目标。官方也明确提供 `@citation-js/core` 和独立插件体系。

还可以考虑做成：

```text
Citation.js = optional peer integration
```

而不是 mslang core 强依赖。

比如：

```js
render(src, {
  citationEngine
})
```

默认仍有当前 lightweight formatter。

用户安装：

```text
@citation-js/core
@citation-js/plugin-csl
```

后再启用高级 CSL。

这是**最轻量的方案**。

如果你的 AI 文献工作台一定会使用 CSL，那么直接作为正式依赖也完全合理。

---

# 14. 0.3 的优先级

我建议严格按：

**第一阶段：学术数据模型。**  
Document metadata + CSL-JSON bibliography 数据模型。

**第二阶段：Citation.js。**  
CitationEngine adapter + CSL citation/bibliography，现有 formatter 保留兼容。

**第三阶段：SourceMap。**  
include / multi-document 后仍能定位原文件。

**第四阶段：脚注语义。**  
同 label 共享编号、多 backlink。

**第五阶段：SemanticModel。**  
outline、section、figure/table/citation relationship、stats。

**第六阶段：Diagnostics。**  
unused bibliography、unreferenced figure/table 等确定性检查。

**第七阶段：第二 Renderer。**  
如果确有需求，再实验 LaTeXRenderer。

---

# 最终定位

如果 0.2 系列解决的是：

> **“mslang 如何正确地解析和渲染？”**

那么 0.3 应该回答的是：

> **“mslang 如何知道自己正在描述一篇学术论文？”**

因此我建议把 0.3 的核心理念定为：

> **Academic semantics first, syntax second.**

Citation.js + CSL 正好非常符合这条路线：**不要让 mslang 自己成为引用格式引擎，而是让 mslang 管理引用的语义关系，让成熟的 CSL 生态负责引用样式。** 这样 0.3 不仅功能更完整，而且不会因为不断加入格式规则而变得臃肿。

这版完成以后，mslang 就会从现在的“功能很强的学术 Markdown DSL”，真正进入“**结构化、可分析、可被 AI 操作的学术文档语言**”阶段。